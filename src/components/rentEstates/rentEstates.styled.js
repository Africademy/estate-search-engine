import styled from "styled-components"

export const RentEstatesWrapper = styled.main`
  width: 100vw;
  height: auto;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  grid-column-gap: 2vw;
  grid-row-gap: 2vw;
  padding: 0 5vw 0;
`
